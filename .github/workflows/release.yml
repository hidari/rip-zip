name: Release

on:
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always

jobs:
  build-release:
    strategy:
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            suffix: .exe
          - os: macos-latest
            target: x86_64-apple-darwin
            suffix: ""
          - os: macos-latest
            target: aarch64-apple-darwin
            suffix: ""
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable
        with:
          targets: ${{ matrix.target }}

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package Binary
        shell: bash
        run: |
          binary_name="rip${{ matrix.suffix }}"
          cd target/${{ matrix.target }}/release
          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            7z a ../../../rip-zip-${{ matrix.target }}.zip $binary_name
          else
            zip ../../../rip-zip-${{ matrix.target }}.zip $binary_name
          fi

      - name: Generate SHA256 checksum
        shell: bash
        run: sha256sum rip-zip-${{ matrix.target }}.zip > rip-zip-${{ matrix.target }}.zip.sha256

      - name: Upload Release
        uses: softprops/action-gh-release@153bb8e04406b158c6c84fc1615b65b24149a1fe # v2
        with:
          files: |
            rip-zip-${{ matrix.target }}.zip
            rip-zip-${{ matrix.target }}.zip.sha256
          generate_release_notes: true

  notify-homebrew:
    needs: build-release
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Homebrew Formula Update
        uses: actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b # v7
        with:
          github-token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          script: |
            await github.rest.repos.createDispatchEvent({
              owner: 'hidari',
              repo: 'homebrew-tap',
              event_type: 'update-formula',
              client_payload: {
                formula: 'rip',
                version: context.ref.replace('refs/tags/', '')
              }
            });
